name: Build HabitBloom APK

on:
  workflow_dispatch:  # 手动触发
  push:
    branches: [ main, master ]
    paths:
      - 'main_kivy.py'
      - 'buildozer.spec'
      - 'src/**'
      - 'kivy_ui/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          python3-pip \
          build-essential \
          git \
          python3-dev \
          openjdk-11-jdk \
          autoconf \
          libtool \
          pkg-config \
          libffi-dev \
          libssl-dev \
          zlib1g-dev \
          libsdl2-dev \
          libsdl2-image-dev \
          libsdl2-mixer-dev \
          libsdl2-ttf-dev
    
    - name: Set up Java
      run: |
        echo "JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64" >> $GITHUB_ENV
        echo "$JAVA_HOME/bin" >> $GITHUB_PATH
    
    - name: Install Buildozer
      run: |
        pip install --upgrade pip
        pip install buildozer cython
    
    - name: Check project structure
      run: |
        echo "检查项目结构..."
        ls -la
        echo "检查主文件..."
        test -f main_kivy.py && echo "✅ main_kivy.py 存在" || echo "❌ main_kivy.py 不存在"
        test -f buildozer.spec && echo "✅ buildozer.spec 存在" || echo "❌ buildozer.spec 不存在"
        test -d src && echo "✅ src 目录存在" || echo "❌ src 目录不存在"
        test -d kivy_ui && echo "✅ kivy_ui 目录存在" || echo "❌ kivy_ui 目录不存在"
    
    - name: Validate buildozer.spec
      run: |
        echo "检查 buildozer.spec 配置..."
        if grep -q "source.main = main_kivy.py" buildozer.spec; then
          echo "✅ 主入口文件配置正确"
        else
          echo "❌ 主入口文件配置错误"
          exit 1
        fi
        if grep -q "requirements = " buildozer.spec; then
          echo "✅ 依赖配置存在"
        else
          echo "❌ 依赖配置缺失"
          exit 1
        fi
    
    - name: Build APK
      run: |
        echo "开始构建 APK..."
        buildozer android debug 2>&1 | tee build.log
        if [ $? -eq 0 ]; then
          echo "✅ 构建成功"
          ls -lh bin/*.apk 2>/dev/null || echo "⚠️  未找到 APK 文件"
        else
          echo "❌ 构建失败，查看日志："
          tail -100 build.log
          exit 1
        fi
    
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: habitbloom-apk
        path: bin/*.apk
        retention-days: 7
    
    - name: Create Release
      if: github.event_name == 'workflow_dispatch'
      uses: softprops/action-gh-release@v1
      with:
        files: bin/*.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
